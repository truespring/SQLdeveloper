database 를 쓰는 이유
정보기억을 위한 공간

무결성(정규화) - 중복을 줄이는 것
역정규화

성능 <--> 무결성

pk(primary key) 는 레코드를 대표하기 위해 존재 (정수 값을 추천 - 포퍼먼스)
2가지 의미 유니크(index), not null - 유일하다(중복 X), 정보가 반드시 들어가야함

var가 붙으면 동적할당
ex) char(10) -> 크기를 미리 할당한다 - 주민등록번호 저장에 용이(00으로 시작되기 때문에 문자열로 저장해야함)
     varchar2(10) -> 10이내로 조절된다
